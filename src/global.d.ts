/// <reference types="react-native/src/types/globals" />

/**
 * Global type declarations for MasarraApp
 *
 * __DEV__ is provided by React Native at runtime but the base tsconfig
 * restricts `types` to ["jest"], preventing TS from seeing the RN global
 * declarations automatically. This reference directive fixes that.
 */
